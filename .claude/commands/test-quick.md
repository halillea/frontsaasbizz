Run quick tests on recently changed files:

1. Check git status to see modified files: `git status --short`
2. Run vitest in related mode: `npm run test -- --run --reporter=verbose`
3. Show pass/fail summary with test names
